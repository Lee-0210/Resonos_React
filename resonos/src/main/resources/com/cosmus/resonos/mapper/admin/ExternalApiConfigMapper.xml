<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cosmus.resonos.mapper.admin.ExternalApiConfigMapper">

  <resultMap id="ExternalApiConfigResultMap" type="com.cosmus.resonos.domain.admin.ExternalApiConfig">
    <id property="id" column="id"/>
    <result property="provider" column="provider"/>
    <result property="apiKey" column="apiKey"/>
    <result property="secret" column="secret"/>
    <result property="enabled" column="enabled"/>
    <result property="createdAt" column="created_at"/>
    <result property="description" column="description"/>
  </resultMap>

  <!-- 전체 목록 조회 -->
  <select id="list" resultMap="ExternalApiConfigResultMap">
    SELECT * FROM external_api_config ORDER BY created_at DESC
  </select>

  <!-- 단건 조회 -->
  <select id="select" resultMap="ExternalApiConfigResultMap">
    SELECT * FROM external_api_config WHERE id = #{id}
  </select>

  <!-- 등록 -->
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO external_api_config (provider, apiKey, secret, enabled, description)
    VALUES (#{provider}, #{apiKey}, #{secret}, #{enabled}, #{description})
  </insert>

  <!-- 수정 -->
  <update id="update">
    UPDATE external_api_config
    SET provider = #{provider},
        apiKey = #{apiKey},
        secret = #{secret},
        enabled = NOT enabled,
        description = #{description}
    WHERE id = #{id}
  </update>

  <!-- 삭제 -->
  <delete id="delete">
    DELETE FROM external_api_config WHERE id = #{id}
  </delete>

  <!-- API 키 설정 업데이트 -->
  <update id="configUpdate">
    UPDATE external_api_config
    SET apiKey = #{apiKey}
    WHERE id = #{id}
  </update>

  <!-- provider별 검색 -->
  <select id="selectByProvider" resultMap="ExternalApiConfigResultMap">
    SELECT * FROM external_api_config
    WHERE provider = #{provider} AND enabled = 1
    LIMIT 1
  </select>

</mapper>
