<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cosmus.resonos.mapper.community.ComVoteMapper">

  <select id="list" resultType="ComVote">
    SELECT * FROM com_vote
  </select>

  <insert id="insert">
    INSERT INTO com_vote(
    )
    VALUES (
    )
  </insert>

  <update id="update">
    UPDATE com_vote
    <set>
      updated_at = now()
    </set>
    WHERE -- key 지정 필요
  </update>

  <delete id="delete">
    DELETE FROM com_vote WHERE -- key 지정 필요
  </delete>

  <update id="completeAll">
    UPDATE com_vote SET -- 수정필드 없음
  </update>

  <delete id="deleteAll">
    DELETE FROM com_vote
  </delete>
</mapper>