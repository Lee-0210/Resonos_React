<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cosmus.resonos.mapper.community.CommentMapper">

  <select id="list" resultType="Comment">
    SELECT * FROM comment
  </select>

  <insert id="insert">
    INSERT INTO comment(
    )
    VALUES (
    )
  </insert>

  <update id="update">
    UPDATE comment
    <set>
      updated_at = now()
    </set>
    WHERE -- key 지정 필요
  </update>

  <delete id="delete">
    DELETE FROM comment WHERE -- key 지정 필요
  </delete>

  <update id="completeAll">
    UPDATE comment SET -- 수정필드 없음
  </update>

  <delete id="deleteAll">
    DELETE FROM comment
  </delete>
  <select id="findByTarget" resultMap="CommentResultMap">
    SELECT * FROM comment WHERE type = #{type} AND target_id = #{targetId} ORDER BY created_at DESC
  </select>
  <!-- 댓글 개수 조회 -->
  <select id="countByUserId" resultType="int">
    SELECT COUNT(*) FROM comment WHERE user_id = #{userId}
  </select>


</mapper>
