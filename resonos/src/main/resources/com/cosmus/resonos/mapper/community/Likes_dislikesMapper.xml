<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cosmus.resonos.mapper.community.Likes_dislikesMapper">

  <select id="list" resultType="Likes_dislikes">
    SELECT * FROM likes_dislikes
  </select>

  <insert id="insert">
    INSERT INTO likes_dislikes(
    )
    VALUES (
    )
  </insert>

  <update id="update">
    UPDATE likes_dislikes
    <set>
      updated_at = now()
    </set>
    WHERE -- key 지정 필요
  </update>

  <delete id="delete">
    DELETE FROM likes_dislikes WHERE -- key 지정 필요
  </delete>

  <update id="completeAll">
    UPDATE likes_dislikes SET -- 수정필드 없음
  </update>

  <delete id="deleteAll">
    DELETE FROM likes_dislikes
  </delete>
</mapper>